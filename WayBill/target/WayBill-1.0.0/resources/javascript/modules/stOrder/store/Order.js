Ext.define('StOrderModule.store.Order', {
    extend: 'Ext.data.Store',
    model: 'StOrderModule.model.OrderModel',
    
    autoLoad: false,
    autoDestroy: true,
    proxy: {
        type : 'ajax',
        actionMethods: { read: 'POST' },
        url : 'services/orderList',//请求
        reader: {
            type: 'json',
            root: 'items',
            idProperty: 'id',
            totalProperty: 'totalCount'
        }
    }
});